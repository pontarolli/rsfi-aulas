# Metadados compartilhados por todas as aulas desta pasta.
# Editar aqui atualiza a capa de TODOS os slides de uma vez.
#
# O campo `institute` abaixo é reescrito automaticamente pelo script
# scripts/set_version.py a cada `quarto render`, que acrescenta o número
# da versão (baseado na contagem de commits do git) a partir de
# `institute_base`. Para mudar o texto fixo da capa, edite `institute_base`
# — não edite `institute` diretamente, ele será sobrescrito.

author: "Ricardo Pasquati Pontarolli"
subtitle: "Redes sem Fio"
institute_base: "Técnico em Redes de Computadores · IFSP Boituva · pasquati@ifsp.edu.br"
institute: "Técnico em Redes de Computadores · IFSP Boituva · pasquati@ifsp.edu.br · v5"
